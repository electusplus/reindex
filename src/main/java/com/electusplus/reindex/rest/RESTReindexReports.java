package com.electusplus.reindex.rest;

import static spark.Spark.get;

public class RESTReindexReports extends ARest {
    @Override
    public void rest() {
//        path("/reindex_reports", () -> {
//            get("/get_task_report/:taskId", (request, response) -> {
//                logger.info("Got request for task report with id: " + request.params(":taskId"));
//                return objectToString(dataWarehouse.getTaskReportById(request.params(":taskId")));
//            });
//
//        });
    }
}